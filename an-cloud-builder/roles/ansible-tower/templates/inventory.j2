[tower]
localhost ansible_connection=local

[database]

[all:vars]
admin_password='{{ cloud_password  | default ('ansible') }}'

pg_host=''
pg_port=''

pg_database='awx'
pg_username='awx'
pg_password='{{ cloud_password | default ('ansible') }}'

rabbitmq_port=5672
rabbitmq_vhost=tower
rabbitmq_username=tower
rabbitmq_password='{{ cloud_password | default ('ansible') }}'
rabbitmq_cookie=cookiemonster

# Needs to be true for fqdns and ip addresses
rabbitmq_use_long_name=false
